{
  "name": "arapy-11labs",
  "version": "0.0.1",
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html --dist-dir dist --public-url ./ && cp -R public/* dist/ && cp .nojekyll dist/",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@elevenlabs/client": "^0.8.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "parcel": "^2.14.4"
  }
}
